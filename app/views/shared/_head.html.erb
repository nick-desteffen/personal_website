<head>
  <title><%= page_title %></title>
  <%= stylesheet_link_tag "application" %>

  <%= csrf_meta_tags %>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Nick DeSteffen's Blog" %>" />
  <meta name="keywords" content="<%= content_for?(:keywords) ? yield(:keywords) : "nick, desteffen, chicago, ruby, rails, ruby on rails, java, j2ee, consultant, IT, rspec, javascript, jquery" %>" />

  <%= auto_discovery_link_tag(:atom, blog_path(:atom)) %>
  <%= favicon_link_tag %>
</head>
