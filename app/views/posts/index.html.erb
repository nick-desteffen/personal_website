<% @posts.each do |post| %>
  <div class="blog_post">
    <h1><%= post.title %></h1>
    <time><%= format_date(post.published_at) %></time>
    <p><%= teaser(post) %></p>
    <p><%= link_to "Read more", blog_post_path(post) %><span class="comment_count"><%= pluralize(post.comments.not_spam.size, "comment") %></span></p>
  </div>
<% end %>

<% content_for :sidebar do %>
  <%= render :partial => "shared/social_media" %>
<% end %>